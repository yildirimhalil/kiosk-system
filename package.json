{
  "name": "kiosk-system",
  "private": true,
  "scripts": {
    "build": "npm run build --workspace=api",
    "lint": "npm run lint:ci --workspace=api",
    "test": "npm run test --workspace=api"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@nestjs/class-transformer": "^0.4.0",
    "@prisma/adapter-pg": "^5.15.0",
    "@prisma/client": "^5.15.0",
    "class-validator": "^0.14.3",
    "pg": "^8.16.3",
    "prisma": "^5.15.0"
  },
  "devDependencies": {
    "@types/pg": "^8.16.0"
  }
}

# Kopyala: cp .env.example .env
# Prisma / PostgreSQL
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/DBNAME?schema=public"

# NestJS (apps/api)
PORT=3000

# JWT (prod'da güçlü secret kullanın)
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d
